import { FadeUpOnScrollDirective } from './fade-up-on-scroll.directive';

describe('FadeUpOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeUpOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
